# Risk Register
> Purpose: known risks, owned and reviewed rather than forgotten.
> Project: bookslot (PRIVATE track)
> Last updated: 2026-08-26 (Session 12 — R-07 revised: the backfill it warns about now exists, so the risk is no longer latent; Session 13 — R-07 re-checked, not further revised: the real Stripe credentials this session's brief claimed were in `.env` were not actually there (D-0035), so no real-traffic evidence bears on this risk yet — it stands exactly where Session 12 left it; Session 14 — R-07's real-Stripe-verification sub-claim closed as a permanently accepted residual risk, not a pending item: the project owner explicitly, permanently descoped ever obtaining real Stripe credentials for this project (D-0036) — see status column; Session 15 — R-07's reconciliation-check mitigation (the half of R-07 that does not require Stripe access) built and tested (D-0037): `mandates:reconcile-backfill`, a 30-minute grace period, hourly-scheduled. Mitigation status moves from "recommended, not built" to "implemented and tested" — the real-Stripe-verification half stays exactly where Session 14/D-0036 left it, unchanged; Session 16 — new entry **R-08**: the Nuxt frontend's real client-side/browser behavior has never been exercised by an actual browser, only replicated at the HTTP-protocol level — tracked here, not left as a sentence in a session handoff that gets superseded)

| ID | Risk | Category | Impact | Likelihood | Mitigation | Status | Review date |
|---|---|---|---|---|---|---|---|
| R-01 | No real design-partner pilot exists yet — every business assumption in `00-project-brief.md` is reasoned, not validated | Business/discovery | High — the whole product could be built against wrong assumptions | High (this is the current state) | Recruit one real pilot studio before investing heavily in MVP feature breadth; treat pilot recruitment as the top-priority next step, not a nice-to-have | Open | Next session |
| R-02 | Multi-tenancy is genuinely new territory for this developer (contrast: `privacy-forge` is single-org) — a cross-tenant data leak would be severe | Security/architecture | High — direct trust failure if it happens in production | Medium (new pattern, not yet implemented or tested) | Dedicated, exhaustive tenant-isolation test suite before pilot readiness (see `01-scope-and-non-goals.md`'s Definition of MVP complete) | Open | Before first real pilot goes live |
| R-03 | Stripe Connect Express onboarding friction may be a genuine adoption blocker for non-technical owners, not just minor friction | Product/adoption | Medium — could stall onboarding entirely for some owners | Unknown (unvalidated) | Learn directly from a real pilot's onboarding experience; have a fallback plan (e.g., hands-on onboarding assistance) ready for the first few pilots | Open | After first pilot onboarding attempt |
| R-04 | Automated reminders may not measurably reduce no-show rate as assumed | Product/business model | High — this is the core value proposition | Unknown (unvalidated) | Measure real before/after no-show rate against a real pilot studio's own prior rate; be willing to revise the value proposition if the effect is small | Open | After pilot has enough appointment volume to measure |
| R-05 | Email/SMS deliverability (reminders landing in spam or not being delivered) undermines the core value proposition silently | Operational | Medium-High — a reminder that doesn't arrive is worse than no reminder feature at all, since the owner believes it's working | Medium | Deliverability tuning work in a future deployment session; monitor delivery/open rates, not just send success | Open | During implementation session that builds reminders |
| R-06 | Price-sensitive target buyer may not tolerate even a modest recurring fee plus transaction percentage | Business model | High — determines whether the business is viable at all | Unknown (unvalidated) | Validate willingness-to-pay directly and early with a real pilot, before building deep into paid-expansion features | Open | After first pilot pricing conversation |
| R-07 | `payment_mandates.stripe_payment_method_id` is nullable-with-backfill (D-0031, amends D-0010) — a deposit can succeed and Stripe can attach a payment method, but the write-back to that column can silently never happen (code bug, webhook delivery failure, downtime, or this endpoint simply never being called for a given booking, e.g. Stripe.js confirms client-side but the browser never calls our backend). **Session 12 (D-0033) built the write-back itself** — `PaymentConfirmationController` now sets this column from a real `PaymentIntentGateway::retrieve()` call. **Session 14: the project owner explicitly, permanently descoped ever obtaining real Stripe credentials for this project (D-0036)** — the "unproven against real traffic" half of this risk will never be closed by a future real-Stripe pass within this project's lifecycle. **Session 15 (D-0037): the reconciliation-check mitigation itself — the half of this risk that never depended on Stripe access — is now built and tested**, closing the "nothing currently detects it" half of this risk's original description. `app/Console` no longer has zero scheduled jobs, as it did through Session 14 | Data integrity / dispute evidence | Medium-High — unchanged: if a dispute later arrives for that appointment's off-session balance charge, the mandate-evidence record D-0010 exists specifically to defend would be silently incomplete at exactly the moment completeness matters most | **Medium (down from Medium-High as of Session 15)** — the write-back code exists, is the default path (D-0033/D-0034), and is fully proven against `FakePaymentIntentGateway`'s simulated failure/latency cases; real Stripe network behavior remains permanently unverified (D-0036, unchanged — a stated, accepted limitation). What changed this session: a silent divergence between our database and Stripe's real state — the scenario this risk exists to describe — is no longer undetected-forever; `mandates:reconcile-backfill` (D-0037) surfaces it, bounded by a reasoned 30-minute grace period, within at most one hourly scheduler run. Likelihood of the underlying divergence occurring is unchanged; likelihood of it going *unnoticed* is now meaningfully lower | **Built and tested (D-0037):** `app/Console/Commands/ReconcilePaymentMandatesCommand.php` (`mandates:reconcile-backfill`), scheduled hourly (`routes/console.php`), flags any `payment_mandates` row with `stripe_payment_method_id` still `NULL` past `config('booking.reconciliation_grace_minutes')` (30 minutes, reasoned from D-0021's confirm-payment token lifetime — see D-0037) via a structured `Log::warning()` carrying tenant/appointment/mandate ids and current appointment/payment status for triage, plus a non-zero exit code as a coarse monitoring-wrapper signal. Tested: `tests/Feature/Console/ReconcilePaymentMandatesCommandTest.php` (orphaned-past-grace flagged; legitimately-mid-flight not flagged; already-backfilled never flagged; cross-tenant coverage proven). **Remaining, real gap, named not assumed solved:** no scheduler process actually runs anywhere yet (`08-deployment-and-operations.md` documents none) — this command is eligible to run but needs a real `php artisan schedule:run` cron entry on deployment, tracked as deployment-story work, not a flaw in the mitigation itself | **Mitigation implemented and tested; real-Stripe-verification half remains a permanently accepted residual risk (D-0036), unchanged** | Wire the still-missing scheduler cron entry into whatever session first actually deploys this project (see `08-deployment-and-operations.md`); real-Stripe verification stays N/A, permanently descoped — do not re-open pending a future key |
| R-08 | The Nuxt frontend (`frontend/`, Session 16) has never been exercised by a real browser — no click, no keystroke, no rendered pixel has ever been observed. Session 16's own verification proved a real, distinct, but narrower claim: that the Laravel backend correctly handles the exact HTTP requests the frontend's code is *written* to send (real CORS headers, the real Sanctum CSRF cookie dance, real booking/confirm-payment responses) — proven by a `curl`-based script replicating those request shapes, not by the frontend's own TypeScript actually executing them. Nothing has ever run `useApi.ts`'s `ensureCsrfCookie()`/`readCookie()` client-side code, Vue's reactivity/step-transition logic in `tenants/[slug]/index.vue`, or any `@click`/`v-model` handler, inside an actual browser. SSR was proven for real (`curl`ing the page shows real backend data server-rendered into the initial HTML) — but SSR is Node-side Vue execution with no hydration, no event handlers, and no client-only code paths engaged; it does not touch this risk's actual concern | Test coverage / verification gap | Medium — a customer-facing flow could be broken in ways no test in this project's suite would catch (a slot-picker click that silently does nothing, a broken v-model binding, a hydration mismatch, the CSRF cookie genuinely failing to attach from real browser cookie storage the way the curl replication assumed) while every other check (typecheck, production build, backend gate, the HTTP-replication proof) stays green | Medium — untested client-side code in a fresh, single-session frontend is a plausible place for a real bug to hide, and this project has no frontend automated test suite (`Vitest`/`Playwright`) and had no browser automation tool available in the session that built it | A real browser-driven pass (manual click-through, or automated via Playwright/similar once available) exercising: slot selection, the mandate consent checkbox gating the submit button, form validation error display, the multi-step state transitions, and the CSRF cookie genuinely being read from real browser storage rather than a script-primed cookie jar. Standing item until performed — not resolved by any amount of further backend-side or protocol-replication testing, which addresses a different claim entirely | Open | Next session that has real browser tooling available, or before this flow is shown to a real customer/pilot, whichever comes first |

## Closed risks

None yet.
