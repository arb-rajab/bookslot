# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Environment / secrets
.env
.env.*
!.env.example
!.env.*.example
# .env.testing carries no real secrets — fixed local-only Postgres
# passwords matching docker/postgres/init/ and a test-only APP_KEY/Stripe
# key — committed so `composer ci:check` works the same for every clone
# and in CI, per 07-testing-strategy.md/08-deployment-and-operations.md.
!.env.testing

# Laravel (backend — added in a future session)
/vendor/
/storage/*.key
/storage/framework/cache/*
/storage/framework/sessions/*
/storage/framework/views/*
/storage/logs/*
/bootstrap/cache/*
composer.phar
.phpunit.result.cache

# Node / frontend (added in a future session)
node_modules/
.nuxt/
.output/
dist/
.cache/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Docker
docker-compose.override.yml
